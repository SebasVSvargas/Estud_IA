
//Filter for tasks, by status (completed, pending) and by priority (high, medium, low)

function Filter() {
    return (
        <div>
            <h2>Filter</h2>
        </div>
    );
}

export default Filter;
